{
  "name": "agents-memo",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prepare": "husky",
    "format": "bin/minify-md -i -r ."
  },
  "devDependencies": {
    "git-cliff": "^2.13.1",
    "husky": "^9.1",
    "lint-staged": "^15.5"
  },
  "lint-staged": {
    "*.md": [
      "bin/minify-md -i -r"
    ]
  }
}
