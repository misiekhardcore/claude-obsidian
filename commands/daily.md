---
description: Append a timestamped bullet to today's daily log in the vault.
argument-hint: <verbatim text>
---
Run `daily` skill. Append `- HH:MM <text>` under `## Captures` in `daily/YYYY-MM-DD.md`. Create directory/file if missing.

Usage: `/daily <verbatim text>`. If no vault: "No vault configured — run /wiki init first."
